name: sophia_path
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0 # version 0.1.0 : initial version , 0.2.0 : created basic chat interface and implemented chat functionality

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  path: ^1.8.0
  provider: ^6.0.0
  intl: 0.18.1
  shared_preferences: ^2.5.4
  image_picker: ^1.2.1
  path_provider: ^2.1.5
  share_plus: ^12.0.1
  google_fonts: ^8.0.0
  curved_navigation_bar: ^1.0.6
  flutter_im_list: ^1.0.0
  http: ^1.6.0
  socket_io_client: ^2.0.3+1
  dotted_line: ^3.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/sp-logo.png
    - assets/courses/
    - assets/sections/
